# Let persisted evidence control completion

Autopilot establishes an immutable Verification Plan before Worker launch and marks work complete only after required deterministic and semantic evidence passes on the integrated result.
Worker statements and session lifecycle events remain observations, while baseline comparison, constrained command execution, bounded repair, and explicit stuck states keep verification independent from execution.
